#include "main.h"

/**
 * _abs - compute the absolute value of a number
 * @i: number to compute it abs
 * Return: integer of the number
 */
int _abs(int i)
{
	if (i < 0)
		return ((-1 * i));
	return (i);
}
